# Local Docker Compose defaults only. Never reuse these values outside a local workstation.
THE_SEARCH_DATABASE_PORT=5432
THE_SEARCH_DATABASE_ADMIN_PASSWORD=local-admin-only
THE_SEARCH_DATABASE_MIGRATOR_PASSWORD=local-migrator-only
THE_SEARCH_DATABASE_APPLICATION_PASSWORD=local-app-only
